---
product_version: V4.4
title: V4.4 业务验证执行计划
status: open
revision: 1
governance_ref: AGP-0.1
next_actor: human
created: 2026-08-08
---

# V4.4 执行计划：真实事件运营验证与研究价值闭环

## 1. 版本定位

**产品版本：V4.4**

核心定位：

> V4.4 = 真实事件运营验证 + 研究价值闭环 + 事件研究记忆。

V4.3 已经解决：

- 研究流程可以稳定运行；
- 自动化链路可验证；
- Evidence 可追溯；
- 异常能够 fail-closed；
- Human / Cursor / Codex 治理闭环已经建立。

V4.4 不再继续证明"系统能不能运行"，而是验证：

> 系统放进真实金融事件以后，是否能够持续产生研究价值、减少人工重复劳动，并把事件经验转化为下一次可以复用的研究记忆。

---

## 2. V4.4 核心业务目标

V4.4 只回答三个问题：

### Goal A：系统是否真的能进入真实事件运营？

即：

> 不是测试样本，而是真实 Macro / Policy / Earnings Event 是否可以完整跑通现有 Research Pipeline。

### Goal B：系统到底创造了什么研究价值？

即：

> 哪些工作已经被自动化，哪些判断 Human 仍然需要介入，AI 输出到底在哪里有用、哪里仍然不足。

### Goal C：系统是否会随着使用形成可复用经验？

即：

> 一次事件结束后产生的研究经验，能否直接帮助下一次类似事件减少重复研究。

---

## 3. V4.4 总体业务闭环

```text
真实事件进入
        ↓
Event Identity
        ↓
正式 Evidence 收集
        ↓
As-of Context
        ↓
Propagation Analysis
        ↓
Pre-event Research Draft
        ↓
Human Review
        ↓
事件发生
        ↓
Actual Result + Market Reaction
        ↓
Post-event Verification
        ↓
Human Feedback
        ↓
Reusable Observation
        ↓
Event Research Memory
        ↓
下一次同类事件复用
```

这条闭环是 V4.4 的唯一主线。

---

## 4. Work A — Real Event Operations

### 目标

验证现有 V4.3 自动化链路在真实金融事件中是否可以持续运行。

重点不是增加功能，而是运行已有能力。

### 4.1 首批事件类型

V4.4 首批事件必须覆盖当前已经成熟的三类能力：

#### Macro

建议事件：

- NFP
- CPI
- PPI
- Retail Sales
- GDP
- Initial Jobless Claims

第一阶段不要求全部覆盖。

优先选择：

```text
NFP
CPI
PPI
```

#### Policy

主要：

```text
FOMC
```

可覆盖：

- 利率决议
- Powell Press Conference
- Minutes

#### Earnings

选择少量具有明显市场影响、资料充分的公司。

Earnings 的定位：

> 它是 Event Type，不是整个系统的中心对象。

第一阶段建议：

- 2–4 家代表性公司；
- 不建立"10 家公司"硬性目标；
- 以事件质量而不是公司数量为准。

---

## 5. 首批真实事件篮子

第一阶段建议：

### 总样本

```text
12–18 个真实 Research Runs
```

建议结构：

```text
Macro      6–8
FOMC       2–3
Earnings   4–7
```

目的是形成足够多样的运行样本，而不是扩大规模。

---

## 6. Research Run 最小生命周期

每个 Event 必须尽量经历：

```text
CREATED
↓
EVIDENCE_READY
↓
PRE_EVENT_ANALYSIS
↓
HUMAN_REVIEW
↓
EVENT_OCCURRED
↓
ACTUAL_CAPTURED
↓
POST_EVENT_REVIEW
↓
CLOSED
```

如果因正式 Evidence 不足不能进入正常路径：

```text
BLOCKED
或
ABSTAIN
```

属于合法结果。

不得为了增加 PASS 数量而降低 Evidence 标准。

---

## 7. Work A 验收标准

Work A 不以"全部 PASS"为成功标准。

主要验收：

### A1 — Pipeline Completion

至少：

```text
80%
```

的 eligible event 可以完整进入既有自动化流程。

### A2 — Evidence Traceability

正式事实：

```text
100%
```

必须可以追溯：

- Source
- Version
- As-of Time

### A3 — Exception Visibility

所有：

```text
BLOCKED
FAIL
ABSTAIN
```

必须具有明确原因。

不得 silent fallback。

### A4 — Human Action Count

正常 eligible event：

目标：

```text
≤2 次主要人工确认动作
```

---

## 8. Work B — Research Value Loop

### 目标

从"流程是否成功"升级到：

> "研究是否有用"。

---

## 9. Human Feedback Schema

每次 Human Review 建议至少记录：

```yaml
feedback:

event_id:

research_quality:
  accuracy:
  completeness:
  usefulness:
  traceability:

human_action:
  accepted:
  modified:
  rejected:

issue_type:
  - missing_evidence
  - weak_context
  - wrong_relationship
  - propagation_gap
  - outdated_information
  - overstatement
  - irrelevant_content
  - missing_counter_evidence

human_comment:

reuse_value:
  high:
  medium:
  low:
```

不要求一次性建设复杂 UI。

第一阶段结构化记录即可。

---

## 10. Human Modification Audit

V4.4 必须回答：

> Human 到底改了什么？

建议统计：

```text
Evidence 修订
Context 修订
传播路径修订
资产反应解释修订
措辞修订
Counter-evidence 补充
结论修订
```

如果大量修改只是文案：

说明研究核心可能已经有效。

如果大量修改集中在：

```text
Evidence
Propagation
Context
```

说明系统核心研究能力仍需加强。

---

## 11. Research Value KPI

V4.4 不用"报告数量"作为核心 KPI。

建议主要观察：

### 效率

```text
Human 平均处理时间
人工确认次数
重复填写字段数
Evidence 人工补充次数
```

### 研究质量

```text
Evidence 可追溯率
Human 接受率
Human 结构性修改率
Post-event 复盘完成率
Counter-evidence 覆盖率
```

### 使用价值

```text
Human usefulness score
同类事件历史记录调用率
历史 Observation 被复用次数
```

---

## 12. V4.4 明确禁止的 KPI

不得将以下指标作为 V4.4 产品 PASS 条件：

```text
股票涨跌命中率
事件方向预测准确率
投资收益率
交易胜率
```

原因：

V4.4 仍然验证的是：

> 研究价值与研究流程。

不是交易 Alpha。

---

## 13. Work C — Event Research Memory

### 目标

解决项目长期遗留问题：

> 历史事件已经结束，但经验没有自动转化为下一次研究可以复用的 Context。

---

## 14. Event Research Memory 内容

每个已闭环事件，建议沉淀：

```yaml
event_memory:

event_identity:

context:
  macro:
  policy:
  liquidity:

important_evidence:

propagation_paths:
  supported:
  rejected:
  uncertain:

market_reaction:

counter_evidence:

human_conclusion:

lessons_learned:

next_event_watchlist:

reusable_observations:
```

---

## 15. Event Research Memory 与现有 Propagation Graph 的关系

二者必须严格区分。

### Propagation Graph

负责：

```text
结构化先验
```

回答：

> 理论上可能通过什么路径传播？

### Event Research Memory

负责：

```text
历史经验与实际证据
```

回答：

> 过去真实事件里，哪些路径真的成立，哪些没有成立？

不得建立第二个平行：

```text
Finance Knowledge Graph
```

避免与 Propagation Graph 产生权威冲突。

---

## 16. 单一事实源原则

V4.4 不建立第二套 Research Registry。

继续保持：

```text
Existing Research Registry
        │
        ├─ Event Identity
        ├─ Evidence
        ├─ Analysis
        ├─ Feedback
        ├─ Post-event Result
        └─ Reusable Observation
```

新的：

```text
Feedback
Event Memory
Analytics
```

应该是：

- Registry 的扩展；
- 派生数据；
- 索引；
- Projection。

不得成为第二套 authoritative source。

---

## 17. Feedback → Improvement 的治理边界

V4.4 可以识别问题。

例如：

```text
连续 5 次出现 missing_counter_evidence
```

系统可以生成：

```text
Improvement Proposal
```

但不能直接：

```text
自动修改 Prompt
自动改变阈值
自动改变 trusted source
自动改变 propagation rule
```

正确链路：

```text
Human Feedback
↓
Pattern Detection
↓
Improvement Proposal
↓
Human Review
↓
Approved Change
↓
新版本
```

因此 V4.4 是：

> Learning-capable system

但暂时不是：

> Autonomous self-modifying system。

---

## 18. V4.4 分阶段执行

### Phase 1 — Baseline

目标：

建立首批真实样本。

建议：

```text
5–6 个 Research Runs
```

先不要扩大。

验证：

- Registry
- Evidence
- Automation
- Human Feedback
- Post-event Review

是否能够真正串起来。

### Phase 2 — Validation

扩大至：

```text
12–18 个 Research Runs
```

覆盖：

Macro / FOMC / Earnings。

开始统计：

- Human effort
- modification pattern
- exception rate
- reuse value

### Phase 3 — Memory Validation

选择已经完成的同类 Event。

验证：

```text
历史 Event Memory
        ↓
下一次同类 Event
```

是否真正减少：

- Evidence 重复查找
- Context 重复编写
- Human 重复判断

---

## 19. V4.4 Release Gate

V4.4 不建议仅依赖测试数量作为 Release Gate。

建议同时满足：

### Gate A — Engineering

```text
自动化链路稳定
无关键数据污染
Fail-closed 保持
Regression PASS
```

### Gate B — Operations

至少：

```text
12 个真实 Research Runs
```

其中覆盖：

```text
Macro
Policy
Earnings
```

### Gate C — Research Value

能够明确回答：

```text
AI 减少了哪些人工工作？
Human 仍然主要在哪些环节修改？
哪些输出 Human 认为真正有用？
```

### Gate D — Memory

至少验证：

```text
2–3 组同类事件
```

历史 Event Memory 确实被下一次研究调用和使用。

---

## 20. V4.4 成功标准

V4.4 成功不等于：

> 系统预测市场成功。

V4.4 成功应该意味着：

> financial-alert-system 已经从一个"能够运行的研究自动化系统"，升级成一个"能够在真实金融事件中持续运行、接受 Human 校准，并积累可复用研究经验的研究产品"。

---

## 21. V4.4 Non-goals

明确不做：

- 自动交易；
- 投资建议；
- 收益率优化；
- 大规模多 Agent 扩张；
- 第二套 Research Registry；
- 第二个 Knowledge Graph；
- 自主修改研究规则；
- 大规模 Prompt 重构；
- 为了 KPI 降低 Evidence 标准；
- 以预测命中率证明产品价值。

---

## 22. 与 AGP 的关系

AGP 不进入 V4.4 业务目标。

关系：

```text
AGP
负责开发治理
Agent / Lease / Evidence / Review

------------------------------

V4.4
负责业务价值
Real Event / Research / Feedback / Memory
```

V4.4 不依赖 AGP-1.0 才能推进。

AGP 当前稳定版本：`AGP-0.1`（已冻结）。

AGP 继续作为旁路治理体系独立演化。

---

## 23. V4.4 当前推荐执行顺序

```text
Step 1
冻结 V4.4 Scope

↓

Step 2
定义 Research Run 模板

↓

Step 3
确定首批 5–6 个真实 Event

↓

Step 4
运行 Phase 1

↓

Step 5
记录 Human Feedback

↓

Step 6
完成 Post-event Review

↓

Step 7
形成第一批 Event Research Memory

↓

Step 8
扩大到 12–18 个真实事件

↓

Step 9
验证 Memory 复用

↓

Step 10
Human 业务验收
```

---

## 24. 第一执行批建议

不建议一次排满整个 V4.4。

首批只建立：

```text
Batch-01
```

建议：

```text
Macro:
NFP
CPI

Policy:
FOMC

Earnings:
2–3 个正式财报事件
```

总量：

```text
5–6 Events
```

跑完以后先复盘，再决定 Batch-02。

---

## 25. 最终原则

V4.4 的核心问题永远应该是：

> 这个系统是否帮助 Human 更快、更可靠、更可追溯地理解真实金融事件？

而不是：

> 又增加了多少功能？

也不是：

> AI 猜对了多少次市场方向？

V4.4 应当成为 financial-alert-system 从工程能力走向真实研究产品的第一个正式业务验证版本。

---

## 附录：执行状态元数据

| 字段 | 值 |
|------|-----|
| product_version | V4.4 |
| status | open |
| revision | 1 |
| governance_ref | AGP-0.1 |
| next_actor | human |
| created | 2026-08-08 |
| source | 本会话由 Human 完整给出，AI 直接落地为 Canonical Live Document |

---

*本文档为 V4.4 的 Canonical Live Document，文件名稳定。修订通过 frontmatter `revision` 字段记录；历史快照归档到 `archive/` 目录。*