---
type: 任务进度
tags: [项目, financial-alert-system, 进度]
created: 2026-07-05
updated: 2026-07-10
---

# financial-alert-system：任务进度

## 当前阶段

**P5 前端可用性收尾** — 图谱页可稳定进入「完整模式」，侧栏 Tab 功能逐步验收。

## 本机路径（2026-07-10 更新）

- 代码：`C:\Users\Administrator\Downloads\financial-alert-system`
- 启动：`start_server.bat` → http://127.0.0.1:8000/propagation.html
- Obsidian：`AI项目控制台/financial-alert-system/`

## 已完成

### 图谱与后端基础（至 2026-07-05）

- [x] 确认项目存在，建立 Obsidian 项目文档体系。
- [x] 确认图谱中心目标：全球风险状态；核心输出篮子：BTC、Nasdaq、Gold、USD、Treasury。
- [x] 第一批节点/边写入 `propagation_graph.json`；健康评分 94（healthy）。
- [x] Target 分层、UI 分组、路径/Case 报告按核心 target 优先。
- [x] 美元/美债/黄金/BTC 二、三层传导与验证指标。
- [x] 数据佐证层第一版：verifier 字典 + market snapshot + evidence 状态。
- [x] Inbox / 日历 / ScenarioReasoner 桥接；`static/ingest/` 模块。

### 数据源与本地 BFF（2026-07-10）

- [x] 本地 BFF：`local_server.js` — `/api/proxy`、`/api/fred/series/:id`、`/api/fred/observations/:id`。
- [x] `verifier_data_fetchers.js` — 9 项验证指标 + Market Tab 状态面板。
- [x] 10Y 美债拍卖尾部 API（Treasury Fiscal Data + FRED DGS10 synthetic tail）。
- [x] `data.js` localhost 走 BFF；`.env` 加载 `FRED_API_KEY`。
- [x] 启动脚本：`start_server.bat`、`start_and_test.bat`；冒烟测试脚本。

### 图谱页加载修复（2026-07-10，本次）

- [x] **`propagation.html` 缺少 `</script>` 闭合标签** — 导致 ~4600 行内联 JS 解析失败，主程序（`loadGraph`、Tab 交互）无法运行。**已修复。**
- [x] `static/propagation_quickload.js` — 2 秒内预览图谱 + 事件列表 + 统计，后台升级完整模式。
- [x] `loadGraph` 优化：侧栏先于 `renderGraph` 加载；静默模式避免重复转圈。
- [x] 顶部 **加载状态指示器**（`appLoadStatus`）：启动中 → 预览模式 → 完整模式。
- [x] `propagation-app-ready` 事件 — quickload 可靠等待主程序就绪。
- [x] 清除空 `custom_propagation_graph` localStorage 缓存逻辑。
- [x] **知识库 Tab**：`loadKnowledgeBase()` + `renderKnowledgeResults()` + 点击定位节点/跳转健康·Case。
- [x] **Market 验证器 E2E**：`scripts/test_verifier_e2e.js` 对 9 项底层源验收 **9/9 passed**；ACM FRED 后备由不存在的 `ACMTP10` 改为 `THREEFYTP10`；市场面板显示可用数/延迟/失败项。
- [x] **拆分内联脚本**：`propagation.html` → `static/propagation_app.js`（约 4800 行）；修复 `buildFlowTimeline` 多余 `}` 导致主程序无法解析的问题。
- [x] **README.md**：启动、环境变量、页面功能、9 项验证器、BFF、常见问题。
- [x] **P1 闭环浏览器验收**：完整模式 chip；Case「FOMC 鹰派」有 BTC 路径；市场刷新 **9/9**；知识库 423 条目。修复状态 chip 竞态（卡在「加载中·主程序」）。
- [x] 验收脚本：`scripts/test_p1_acceptance.js`（静态 7/7）、`scripts/test_verifier_e2e.js`（9/9）。
- [x] **日历事前/事后**：补 2026-07 非农/PPI/零售；窗口含近7天已过；结果面板展示摘要；`data.js` timeline 优先读 `CONFIG.MAJOR_EVENTS`；FOMC 默认映射 `fed_hold`。
- [x] **Inbox 归类确认流（2026-07-10）**：选节点 →「确认并分析路径」；建议按钮显示图谱中文标签；状态徽章（待确认/已确认/已分析）；「示例突发」注入 3 条 demo；API `/api/ingest/seed-demo`；浏览器冒烟 **10/10**。
- [x] **Case 库扩展（2026-07-10）**：`static/case_catalog.js` 共享目录；新增 **非农偏热 / CPI 超预期 / PPI 偏强**（保留 FOMC）；Case Tab 下拉切换；回测跟当前 Case；健康准备度 4/4=100%；冒烟 **23/23**。
- [x] **P2 Alert ↔ 图谱桥接（2026-07-10）**：`alert_graph_bridge.js` 按维度/关键词建议节点；Dashboard「看传导」深链；Propagation `?from=alert&event=&case=` 自动定位路径 + 横幅切换建议；修复 history `rows` 导致告警区不显示。冒烟 **10/10**。
- [x] **Alert → Inbox 入库（2026-07-10）**：Dashboard「入 Inbox · 事前/事后」；`EventIngestor.addFromAlert` + `/api/ingest/from-alert`；深链 `?inbox=1&tab=inbox` 自动入库并打开 Inbox；横幅也可入库。冒烟 **9/9**。

## 进行中

- [x] **浏览器验收完整模式** / 市场 9/9 / 知识库 / Case
- [x] **日历事前/事后闭环**（2026-07-10）：近窗事件 + 面板内报告结果 + MAJOR_EVENTS 统一读取
- [x] **Inbox 归类确认流**（2026-07-10）：扫描/示例 → 选节点 → 确认分析 → 跳转路径
- [x] **Case 库扩展**（2026-07-10）：FOMC + NFP + CPI + PPI
- [x] **P2 Alert ↔ 图谱桥接**（2026-07-10）：Dashboard CTA → 深链路径/Case
- [x] **Alert → Inbox**（2026-07-10）：告警入库后可走事前/事后

## 下一步（优先级）

| # | 任务 | 说明 |
|---|------|------|
| 1–10 | ✅ | 至 Alert↔图谱 + Inbox 入库 |
| 11 | 可选 | 推送；更多历史数据事件；事前/事后结果常驻面板 |

## 已知限制

| 功能 | 状态 |
|------|------|
| 预览模式 | 仅图谱缩放/列表展示，不可点选事件 |
| 完整模式 | 需 `start_server.bat` + 等待主程序（通常 10–30s） |
| 知识库 Tab | ✅ 可搜索节点/边/Schema/Case/健康/工具 |
| 验证器实时刷新 | ✅ BFF 层 9/9；浏览器「刷新全部」需完整模式 |
| AI 推演/报告 | 需 Dashboard 配置 DeepSeek Key |

## 阻塞

- 无技术阻塞；需用户在本地保持 `start_server.bat` 运行并硬刷新验证。

## 验收清单（本次）

1. 运行 `start_server.bat`，访问 http://127.0.0.1:8000/propagation.html
2. `Ctrl+Shift+R` 硬刷新 → chip 变为 **「完整模式」**
3. 侧栏 **知识库**：可搜索、有条目
4. 侧栏 **市场** → **刷新全部**：应显示约 `9/9` 可用（或接近）
5. 命令行复测：`node scripts/test_verifier_e2e.js` → `9/9 passed`

---

*最后更新：2026-07-10 — 日历事前/事后闭环*
