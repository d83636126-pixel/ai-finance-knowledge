# 统一研究工作台 UW0：现场基线与迁移台账

- 日期：2026-07-15
- 范围：**仅 UW0**；未改根入口；未进入 UW1；未删旧仪表盘
- 本地服务：临时 `:8765`（`:8000` 被 Docker backend 占用导致 hang）
- Git：`master` @ `ffa0093`（dirty，见归属表）

## 1. Git 冻结

| 项 | 值 |
|---|---|
| 分支 | `master` |
| HEAD | `ffa0093` — `feat(micro-layer): restore research overview dashboard and archive legacy cards` |
| 远程 | 跟踪 origin（本轮未 push） |
| 工作区 | dirty（C10 语义 + UX 收口 + FRED 统一等未提交） |

### 未提交改动归属（禁止 UW 覆盖）

| 归属 | 路径（代表） |
|---|---|
| C10 证据语义 | `scripts/lib/macro_evidence_loop.js`, `fred_source_status.js`, `micro-layer/adapters/macro_context_adapter.js`, `domain/micro_path_status.js`, `domain/micro_transmission_engine.js`, `contracts/*` |
| C10-G / UX | `micro-layer/application/authenticity.js`, `ui/overview_dashboard.js`, `ui/ux_labels.js`, `ui/workbench.css`, `ui/workbench_app.js`, `tests/smoke_ux_*`, `smoke_c10_cross_layer_*` |
| 市场/验证器 | `static/ingest/verifier_data_fetchers.js`, `market_snapshot_updater.js`, `verifier_data_dictionary.json`, `scripts/refresh_market_snapshot_cli.js` |
| 状态/来源 | `scripts/generate_project_status.js`, `build_macro_source_status.js`, `local_server.js`, `static/research_workbench.js`, `project_status.json` |
| 垃圾/离线日志（勿提交除非用户要求） | `_workbench_check_logs/`, `micro-layer/_tmp.html`, `workbench_full_check.json` |

**回退路径（不执行破坏性命令）：** 保留四入口；新壳仅加 `/workbench.html`；UW7 前不切 `/index.html`；旧页可继续用；dirty 区按上表归属，不整文件覆盖。

## 2. 四个现有入口（截图）

截图目录：`04_研究工作台/02_重构执行/_uw0_baseline_2026-07-15/`

| # | URL | 作用 | 截图 |
|---|---|---|---|
| 1 | `/index.html` | 旧根仪表盘（告警/时间线/评分/信号/情绪/相关/系统） | `01-index-dashboard.png` |
| 2 | `/propagation.html` | 宏观推导 + 今日总览研究工作台 | `02-propagation.png` |
| 3 | `/micro-layer/index.html` | 微观研究总览默认入口 | `03-micro-index.png` |
| 4 | `/micro-layer/workbench.html` | 微观工作台镜像（同脚本栈） | `04-micro-workbench.png` |

**`/workbench.html`（根）尚不存在** — UW2 新建。

## 3. 可复用组件（禁止重做）

| 能力 | 复用路径 |
|---|---|
| 微观总览五问 / 三分栏 | `micro-layer/ui/overview_dashboard.js` + `ux_labels.js` + `authenticity.js` |
| 微观工作台编排 | `micro-layer/ui/workbench_app.js` + `application/build_micro_workbench_view.js` |
| 传导图 | `micro-layer/ui/transmission_graph.js` |
| 证据检视 | `micro-layer/ui/evidence_inspector.js` |
| 观察回放 | `micro-layer/ui/observation_timeline.js` |
| 模式导航 | `micro-layer/ui/research_nav.js` |
| 宏观研究壳 | `static/research_workbench.js` + `propagation_app.js` + `deduction_dashboard.js` |
| 市场刷新/验证器 | `static/ingest/market_snapshot_updater.js`, `verifier_data_fetchers.js` |
| RSS/Inbox | `static/ingest/event_ingestor.js`, `inbox_store.js`, `CONFIG.RSS_FEEDS` |
| 宏观证据链产物 | `artifacts/macro_*_latest.*`, `micro_macro_context.json` |
| 微观采集 | `artifacts/micro_collect_snapshot_latest.json`（源平面 `F:\quant-system\data\micro_collect`） |

适配原则：统一壳通过 iframe/适配器挂载，不复制引擎与契约。

## 4. 旧仪表盘功能迁移台账

处置：`迁入` / `折叠` / `条件保留` / `归档`

| 旧功能 | 处置 | 新位置 / 条件 | 回退 |
|---|---|---|---|
| 当前关注 / 告警 `#alertSection` | 迁入 | 实时总览 + 新闻；改为可追溯研究关注项 | 保留 index 深链 |
| 宏观时间线 `#timelineChart` | 迁入并简化 | 新闻与历史回放；实验视图（像素/日历/地铁等）归档 | index 仍可开 |
| 事件分析 `#analysisCard` | 迁入 | 新闻详情 + 宏观推导；事实/模型分栏 | 同上 |
| 活跃/紧急/简报计数 | 折叠 | 总览摘要条，可点回明细 | 摘要条隐藏可回 index |
| `#microLayerContainer` 摘要 | 迁入 | 微观验证模式；复用 micro overview | `/micro-layer/*` |
| 评分全景 `loadScores` | 条件保留 | 需定义/输入/时间/版本齐全，否则 UW5 归档 | 条件门禁未满足前留 index |
| AI 信号 `loadSignals` | 条件迁入（改名） | 「研究发现/待验证命题」；禁止交易措辞 | 改名适配失败则归档 |
| RSS/预热/预算/系统状态 | 折叠 | 系统健康；异常才全局提示 | health 面板 |
| 绩效/准确率/混淆矩阵 | 迁移 | 历史回放 / 方法论 | 不占实时首屏 |
| 叙事情绪 `loadSentiment` | 条件保留 | 有真实来源+窗口+更新时间才进微观；否则归档 | UW5 验真 |
| 相关性热图 `loadCorrelation` | 条件保留 | 同上 | UW5 验真 |
| 设置 / API Key 模态 | 迁移 | 系统健康→配置；**绝不回显密钥** | 设置仍可从 health 开 |
| 重复弹窗/空占位/内部字段 | 归档 | 切根后去引用，不立即删源 | 源文件留到 UW8 |
| 传播图整页 `propagation.html` | 迁入（复用） | 统一壳「宏观推导/传导」适配 | 兼容深链 |
| 微观五模式工作台 | 迁入（复用） | 统一壳「微观/传导/证据/历史」 | 兼容深链 |

**台账完整性：** 上述行均有去向；无「去向不明却准备切根」项。

## 5. 数据/API 现场结论（UW1 前置）

| 层 | 现状 | UW1 风险 |
|---|---|---|
| 真 BFF | `local_server.js`：`/api/proxy`, FRED, translate, archive | 可用 |
| 根仪表盘大量 `/api/overview|scores|sentiment|…` | **客户端 stub**，非服务端契约 | 正式实时流不可依赖 stub |
| 新闻 | 无 `/api/news`；RSS→Inbox | 必须建真相层与去重 |
| 市场快照 | `market_snapshot.json` + refresh CLI | 角色/新鲜度字段需统一 |
| 宏观证据 | artifacts 链已具备 C10 语义 | 不得回退 |
| 微观采集 | collect snapshot + authenticity | 占位不得进正式判断（已有） |

## 6. UW0 门禁结论

| 门禁 | 结果 |
|---|---|
| 旧功能均有去向 | ✅ 通过 |
| 现场改动均有归属 | ✅ 通过（C10/UX 与垃圾文件分层） |
| 回退路径可执行 | ✅ 四入口保留 + 根 workbench 未建 + 不切根 |
| 浏览器四入口截图 | ✅ 已存 `_uw0_baseline_2026-07-15/` |

**UW1 前置：可进入。**  
下一阶段只做实时事实与新闻真相层字段/判定；仍不建壳、不切根、不删页。

## 7. 未做（故意）

- UW1+ 任意实现
- `/workbench.html` 创建
- `/index.html` 替换
- 删除旧仪表盘源文件
- 覆盖 C10 / 微观 UX dirty 改动
