# 统一研究工作台 UW2：统一外壳

- 日期：2026-07-15
- 范围：**仅 UW2**；新建根级 `/workbench.html`；**未切换** `/index.html`；未删旧页
- 冒烟：`npm run smoke:unified-workbench`、`npm run smoke:realtime-truth`

## 1. 交付物

| 路径 | 作用 |
|---|---|
| `workbench.html` | 统一壳入口 |
| `static/unified_workbench.js` | 八模式、URL 上下文、iframe 适配 |
| `static/unified_workbench.css` | 壳层样式 |
| `scripts/smoke_unified_workbench.js` | 模式/直链/未切根门禁 |

## 2. 八模式

| mode | 实现 |
|---|---|
| `overview` | 原生五问骨架 + 嵌入 `micro-layer/index.html?mode=overview` |
| `news` | 原生；Inbox + RealtimeTruth 预览（正式流 UW3 深化） |
| `macro` | iframe → `propagation.html` |
| `micro` | iframe → `micro-layer/index.html` |
| `transmission` | iframe → `micro-layer/workbench.html?mode=transmission` |
| `evidence` | iframe → `…?mode=evidence` |
| `history` | iframe → `…?mode=observation` |
| `health` | 原生；验证器 role/freshness/availability；默认导航弱化 |

上下文：`?mode=&event_id=&as_of=&symbol=`；`replaceState`/`pushState` + `popstate` 刷新可恢复。

## 3. 适配原则

- 不复制微观引擎 / 传播图业务逻辑。
- iframe 透传 `event_id` / `as_of` / `symbol` / `embed=1`。
- 兼容链：旧仪表盘、propagation、micro index/workbench 均保留。

## 4. 门禁

| 项 | 结论 |
|---|---|
| 模式可直链与刷新恢复 | 通过（smoke 读 URL） |
| 上下文透传到 frame URL | 通过 |
| 旧入口仍可用 | 通过（compat 链接 + index 仍在） |
| 根入口未切换 | 通过（index 无自动跳 workbench） |

**UW2 通过 → 可进入 UW3（实时总览与新闻事件流）。**
