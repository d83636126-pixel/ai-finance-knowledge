---
title: 统一研究工作台 SEC0–GATE1 执行记录
date: 2026-07-16
project: financial-alert-system
type: 执行报告
status: partial_complete
phases_done: [SEC0, RT1, TRUTH1, CTX1, GATE1, UX2_partial]
release_gate: blocked
cloud_gate: blocked
next: UW3-R（需真实浏览器 RSS e2e）→ UW4-R → UW6-R → UW7-R → UW8-R T0
tags: [研究工作台, SEC0, RT1, TRUTH1, GATE1]
---

# 统一研究工作台 SEC0–GATE1 执行记录

> 依据 [[统一研究工作台_审核阻断修复与重新验收计划_2026-07-16]]；用户授权连续执行且阶段间无需确认。

## 已完成

### SEC0 安全封口 — PASS（冒烟）

- `scripts/lib/static_public_policy.js`：`path.relative` 边界 + 显式拒绝/扩展名白名单
- `local_server.js`：拒绝 `.env` / `.git` / `local_config.js` / `scripts` / `node_modules` 等；proxy/translate 限流、并发、响应大小；LAN 强制 token；`/api/health/credentials` 仅布尔
- 全入口移除 `static/local_config.js`；传播图不再保存浏览器 FRED key
- 冒烟：`npm run smoke:sec0-static-security` **PASS**

### RT1 真实采集控制器 — 已落地（待浏览器 e2e）

- 新增 `static/ingest/realtime_controller.js`（单飞、退避、可见性降频）
- `index.html` / `workbench.html` 加载 `event_ingestor.js` + controller；启动即 RSS + 市场刷新
- 失败不得把 restored/stale 写成 live

### TRUTH1 真实性契约 — PASS（单元冒烟）

- `assessNewsEligibility` 拆分 archive vs realtime；manual/alert/symptom → derived
- RSS `source` = 具体 `feed_id`，`channel` = rss_zh/rss_en
- 首屏市场标签禁止 stale 显示 live
- `smoke:realtime-truth` / `smoke:uw3-overview-news` **PASS**

### CTX1 — 契约已引入

- `static/ingest/research_context.js`：`event_id` / `node_id` / `as_of` / `symbol`
- 统一壳 `readContext` 接入；下游 mismatch API 可用
- 完整跨层消费仍需 UW4-R 真实事件证明

### GATE1 — PASS（冒烟）

- 无 evidence bridge → `missing` + `blocks_confirmed=true`（fail-closed）
- 传导引擎尊重 `blocks_confirmed`，禁止 legacy 伪 support 进 confirmed
- 浏览器 `MicroExecutionGate` 不再挂载 `grantHumanAuthorization` / `setKillSwitch`
- `smoke_macro_evidence_status` / `smoke:micro-transmission-engine` **PASS**

### UX2（部分）

- 删除「API Key 仍走旧仪表盘」死文案
- 凭据改为服务端布尔说明

## 冒烟汇总（本轮）

| 项 | 结果 |
|---|---|
| smoke:sec0-static-security | PASS |
| smoke:realtime-truth | PASS |
| smoke_macro_evidence_status | PASS |
| smoke:ux-authenticity | PASS |
| smoke:unified-workbench | PASS |
| smoke:uw3-overview-news | PASS |
| smoke:uw4-closed-loop | PASS |
| smoke:micro-transmission-engine | PASS |

## 仍阻断 / 未完成

| 项 | 原因 |
|---|---|
| UW3-R | 需 fresh browser 真实 RSS 请求与 Inbox 入库证据工件 |
| UW4-R | 需同一真实 event_id 五层闭环 |
| UW6-R | 需新建 v2 门禁（内容级五问，非标题匹配） |
| UW7-R | 回退目标须 `git cat-file` dry-run；归档文件已删 |
| UW8-R | 24/72h 观察必须从稳定 commit 重新计时，本会话无法「完成」时长 |

## 发布口径

**release_gate / cloud_gate = blocked**（SEC0 冒烟通过 ≠ 对外发布许可；真实 e2e 与观察未完成）。

## 关联代码（摘录）

- `local_server.js` · `scripts/lib/static_public_policy.js` · `scripts/smoke_sec0_static_security.js`
- `static/ingest/realtime_controller.js` · `realtime_truth.js` · `research_context.js` · `event_ingestor.js`
- `static/unified_workbench.js` · `index.html` · `workbench.html`
- `micro-layer/adapters/macro_context_adapter.js` · `micro_execution_gate.js` · `domain/micro_transmission_engine.js`
