---
title: 统一研究工作台 UW3-R 真实新闻复验
date: 2026-07-16
project: financial-alert-system
type: 复验报告
status: BLOCK
criteria: UW3-R
artifact: artifacts/uw3_realtime_e2e_v2.json
tags: [UW3-R, 真实新闻, BLOCK]
---

# UW3-R：真实新闻与实时总览复验

> 前置 SEC0 / RT1 / TRUTH1 代码已落地。本报告按计划诚实登记，**不涂绿**。

## 结论：**BLOCK**

| 检查 | 结果 |
|---|---|
| fresh browser 发起 RSS / proxy 请求 | **是**（`rss_request_count=14`） |
| 市场相关请求 | **是**（`market_request_count=19`） |
| Inbox 真实入库 | **否**（`inboxLen=0`） |
| ≥1 条正式新闻（source/url/时间/role/freshness） | **否**（`formal_news_count=0`） |
| 阶段可否 PASS | **否**（0 正式新闻不得 PASS） |

工件：`artifacts/uw3_realtime_e2e_v2.json`（`pass=false`）。

## 已观察到的进步（相对审核基线）

- 根页已加载 `RealtimeController`，空浏览器会打出 Fed/Reuters/BBC/中新网/新华等 proxy 请求（不再是「RSS 请求 0」）。
- 市场 Yahoo/Fiscal/eco3min 等请求同步出现。

## 阻断原因（本轮）

1. 首轮探测时页面评估 `hasEventIngestor=false`（`const` 未挂 `window`）；随后已改为 `globalThis.EventIngestor` 导出，**待重跑 e2e**。
2. 上游部分 RSS 返回 401/502；关键词过滤曾丢弃无命中条目——已对官方 feed 放宽入库，**待重跑验证**。
3. 按计划：正式新闻为 0 → 阶段 **BLOCK**。

## 下一步

重跑 Playwright e2e；若 `formal_news_count≥1` 且字段齐全，再升级为 PASS/CONDITIONAL 并写新时间戳工件（不回填本文件时间）。
