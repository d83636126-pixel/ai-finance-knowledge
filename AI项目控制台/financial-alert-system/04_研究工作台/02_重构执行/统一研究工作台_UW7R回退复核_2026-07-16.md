---
title: 统一研究工作台 UW7-R 回退复核
date: 2026-07-16
project: financial-alert-system
type: 复验报告
status: BLOCK
criteria: UW7-R
artifact: artifacts/uw7_rollback_drill_v2.json
head: ffa0093900c0a7c3040d9c0be0fc585259c3316a
tags: [UW7-R, 回退, BLOCK]
---

# UW7-R：根入口与回退复核

## 结论：**BLOCK**

| 检查 | 结果 |
|---|---|
| 工作树根入口为统一壳 | **是**（未提交） |
| HEAD `index.html` 为统一壳 | **否**（仍为旧仪表盘内容） |
| `archive/index_dashboard_legacy_…` 在 Git | **否**（`git cat-file` 失败） |
| 文件级 Copy-Item 回退 | **不可用**（文件已删） |
| dry-run 可执行且哈希一致 | **否** |

工件：`artifacts/uw7_rollback_drill_v2.json`。

## 含义

- 根切换**已实施于工作树**，但**未稳定提交**，且旧盘归档**从未进入 Git 历史**。
- 按计划：回退目标不存在 / dry-run 失败 → UW7-R **一律 BLOCK**。
- 候选恢复：仅能从工作树备份、回收站或未来从 `git show HEAD:index.html` 导出「切根前 HEAD 仪表盘」到临时目录比对——**不得覆盖正式根**（脏树）。

## 下一步

1. 用户明确允许后，按白名单切片提交统一壳（仍保留回退说明）。
2. 从可信备份重建只读旧版基线并记录 SHA256，再重跑 dry-run。
