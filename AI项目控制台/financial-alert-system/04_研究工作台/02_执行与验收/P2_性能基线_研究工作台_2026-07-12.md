---
type: 性能基线
project: financial-alert-system
tags: [项目, financial-alert-system, 性能, P2]
created: 2026-07-12
updated: 2026-07-12
status: done
---

# P2 性能基线 · 研究工作台 · 2026-07-12

命令：`npm run perf:workbench`  
产物：`F:\financial-alert-system\artifacts\perf_baseline_research_workbench.json`  
测量时刻：`2026-07-12T07:58+`（优化后再测通过）

## 指标 vs 目标

| 指标 | 目标 | 实测（median / 首屏） | 判定 |
|---|---:|---:|---|
| 总览主要内容可见 | ≤ 2000 ms | **1136 ms** | ✅ |
| 聚焦图显示完成 | ≤ 1000 ms | **10 ms**（热路径；首次样本同量级） | ✅ |
| 一级导航切换 | ≤ 300 ms | overview/events **18 ms**，fullgraph **~66 ms** | ✅ |
| 报告展开/折叠（代理） | ≤ 300 ms | **57 ms** | ✅ |
| 分栏拖动 | 无明显卡顿 | **154 ms** 完成拖拽模拟 | ✅ |
| 聚焦 vs 全图切换 | 聚焦不慢于探索开关量级 | focus 10 ms / explore ~16+ ms | ✅ |
| 模式切换重复加载图谱 | 0 | **0** 次 `propagation_graph` 重拉 | ✅ |
| JS page error | 0 | **0** | ✅ |

## 场景勾选

- [x] 首次打开今日总览
- [x] 进入聚焦图
- [x] 聚焦 ↔ 全图往返
- [x] 拖动分隔线
- [x] 打开/关闭报告（按钮代理）
- [x] 资产反向归因点击
- [x] evidence 状态读取（总览可信度，15s 缓存）

## 优化前后

| 项 | 前 | 后 |
|---|---|---|
| 测量方法 | 聚焦轮询最多垫高 ~1s | 等到 `isFocusGraphActive` |
| 总览 graph/exposure | 每次全拉 + exposure 重试 | **30s 内存缓存** |
| credibility / snapshot | 每次 `_fetchCredibility` | **15s 缓存** |
| 模式切换 graph 重载 | 0（已满足） | 0 |
| 基线判定 | fail（聚焦 1138ms 被测量垫高） | **pass** |

## 网络与错误

- page errors：无
- 模式切换期间：`propagation_graph` 重拉 **0**；`market_snapshot` 偶发 1（可接受）
- 首次加载仍会拉 graph + snapshot（正常冷启动）

## 说明

- 「聚焦明显快于全图」按**切换耗时**量化；全图探索的力导向布局帧成本未做 FPS 采样，若需可在 P2.1 补 Chrome Performance 录制。
- 本轮不扩功能；缓存仅短时，刷新 evidence 后 15–30s 内总览可能略滞后，可接受。

## 关联

- [[研究工作台后续整改计划_2026-07-12]]
- 脚本：`scripts/perf_research_workbench.js`
