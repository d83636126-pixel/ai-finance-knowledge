---
type: 图谱治理记录
tags: [项目, financial-alert-system, 图谱, 健康检查, 治理]
created: 2026-07-05
updated: 2026-07-05
status: implemented
---

# 图谱健康治理 2026-07-05

## 背景

新增全球风险节点、第一批传导边、target 分层和 UI 支持后，图谱健康状态为：

```text
score: 88
status: watch
warnings: 7
errors: 0
```

主要 warning 是旧 verifier 节点缺少 `subtype`。

## 本次治理

补全以下旧验证节点的 `subtype`：

```text
active_addresses -> onchain_activity
hash_rate -> network_security
mempool_fees -> network_activity
runes_activity -> protocol_activity
housing_starts -> us_housing_data
industrial_production -> us_macro_data
consumer_sentiment -> us_sentiment_data
```

## 校验结果

治理后：

```text
score: 94
status: healthy
errors: 0
warnings: 0
isolated_nodes: 0
cycles: 0
nodes: 113
edges: 252
```

## 剩余 info 项

健康检查仍有 info 级建议，主要是：

- 部分旧 `verifies` 边建议补 `time_lag_hours`。
- 部分旧中间节点没有入边，被标记为 unexpected source。

这些不影响当前 healthy 状态，可以后续逐步治理。

## 下一步

- 可继续补 `verifies` 边的 `time_lag_hours`。
- 可检查 unexpected source 节点是否应改为 event / verifier，或补入边。
- 可进入页面体验检查和核心 target 输出验证。

