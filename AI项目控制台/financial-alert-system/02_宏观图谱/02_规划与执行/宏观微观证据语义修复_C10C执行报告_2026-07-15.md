# 宏观—微观证据语义修复 C10-C 执行报告

- 日期：2026-07-15
- 范围：仅 C10-C（上下文新鲜度 ↔ 宏观证据状态分离）；**未进入 C10-D**

## 改动文件

- `micro-layer/adapters/macro_context_adapter.js` — `deriveMacroEvidenceStatus`；`context_freshness` / `macro_evidence_status` / `overall_research_gate`
- `micro-layer/domain/micro_path_status.js` — 宏观证据门禁：blocking 时禁止 `confirmed`
- `micro-layer/domain/micro_transmission_engine.js` — 宏观边按证据状态裁决；readiness 同步降级
- `micro-layer/contracts/micro_transmission_contract.js` — 契约：`macro_evidence_status∈{stale,missing,insufficient,invalid}` ⇒ 不得 `confirmed` / `ready`
- `micro-layer/application/build_micro_workbench_view.js` — overview 暴露三分栏状态字段
- `micro-layer/tests/smoke_macro_evidence_status.js`（新建）
- `micro-layer/tests/smoke_micro_transmission_engine.js`（+4 情景）

## 硬门禁

```text
macro_evidence_status ∈ {stale, missing, insufficient, invalid}
→ path_status != confirmed
→ readiness = not_ready
```

文件年龄新鲜 ≠ 宏观证据支持。无证据桥字段的 legacy fixture 不启用本门禁。

## 实测（当前 `artifacts/micro_macro_context.json`）

| 字段 | 值 |
|---|---|
| context_freshness | fresh |
| macro_evidence_status | stale |
| blocks_confirmed | true |
| path_status | partially_confirmed（非 confirmed） |
| readiness | not_ready |
| research_stance | insufficient |

## 测试

- `smoke_macro_evidence_status` PASS
- `smoke_micro_transmission_engine` 16/16 PASS
- `smoke:micro-workbench` PASS
- `smoke:micro-transmission` PASS

## 未做

- C10-D FRED/来源一致性
- C10-E 重跑证据链
- C10-F 页面分栏 UI 强化（view model 字段已就绪）
- C10-G 端到端五情景验收

## 建议下一步

进入 **C10-D**：统一 FRED 与来源健康状态表达。
