# 宏观—微观证据语义修复 C10-A / C10-B 执行报告

- 日期：2026-07-15
- 范围：仅 C10-A 冻结基线 + C10-B BTC 证据语义；**未进入 C10-C**
- HEAD 基线：`ffa0093`（另有未跟踪本地日志文件，未纳入本切片逻辑改动）

## C10-A 基线冻结

- 产物目录：`artifacts/c10a_baseline_2026-07-15/`
  - `macro_path_evidence_latest.json`
  - `macro_source_status_latest.json`
  - `micro_macro_context.json`
  - `macro_evidence_report_latest.md`
  - `market_snapshot.json`（含错误别名的修复前副本）
  - `baseline_note.json`
- 复现证据（修复前）：
  - `btc_price.current_value = 11`，`note = Farside…(M USD)`，`aliased_from = btc_etf_flow_verifier`
  - 宏观聚合：`support=0 against=0 missing=1 expired=6`
  - `micro_macro_context.current=true / stale=false`（文件新鲜 ≠ 路径确认；C10-C 待修）
- 基线命令：`smoke:macro-evidence-loop` / `micro-macro-context` / `micro-macro-bind` / `micro-transmission-engine` / `micro-workbench` 均 PASS（修复前）

## C10-B BTC 语义修复

### 改动文件

- `scripts/lib/macro_evidence_loop.js` — 删除 `btc_price→btc_etf_flow_verifier`；`VERIFIER_SEMANTICS` / `aliasCompatible` / `sanitizeVerifierSlot`
- `scripts/refresh_market_snapshot_cli.js` — 新增 `fetchBtcSpotPrice`（Binance+Coinbase+Yahoo）；禁止价格←流量别名
- `verifier_data_dictionary.json` — `btc_price` 原生 USD；ETF 标注 `M USD` / `auxiliary_daily`
- `static/ingest/verifier_data_fetchers.js` — 浏览器侧 `btc_price` 抓取
- `scripts/smoke_macro_evidence_loop.js` — 契约断言
- `market_snapshot.json` — 清除损坏别名后写入原生现货价

### 验收结果

| 检查 | 结果 |
|---|---|
| `btc_price` 合理美元价 | **64786** USD（Binance） |
| ETF 独立 | `btc_etf_flow_verifier=11` M USD，未冒充价格 |
| 跨对象别名被拒 | `aliasCompatible(btc_price, etf)=false`；损坏槽位 sanitize→null |
| 缺 ETF 不致 BTC 价格缺失 | 价格为独立 job/字段 |
| smoke:macro-evidence-loop | PASS |

## 未做（按清单停止）

- C10-C：上下文新鲜度 vs 宏观证据状态分离（微观 `confirmed` 冲突仍在）
- C10-D～G：FRED 一致性、重跑证据链、页面分栏、端到端五种情景

## 建议下一步

进入 **C10-C**：在 adapter/engine/path_status 中传入 `macro_evidence_status`，禁止「宏观过期 + 微观支持 → confirmed」。
