# 宏观—微观证据语义修复 C10-E 执行报告

- 日期：2026-07-15
- 范围：仅 C10-E（重跑宏观证据链）；**未进入 C10-F**

## 执行命令

```powershell
npm run run:macro-evidence-loop -- --skip-m0
```

链内步骤：`refresh:evidence` → `build:macro-scenario` → `build:macro-path-evidence` → `build:macro-source-status` → `bridge:macro-micro` → `report:macro-evidence`

## 刷新结果（`evidence_refresh_report.json`）

| 指标 | 值 |
|---|---|
| connected | 11/11 |
| market_refresh | true |
| FRED mode | last_good_snapshot |
| cannot_live_confirm | true |

## 关键槽位抽检

| 槽位 | 值 / 来源 | 语义 |
|---|---|---|
| `btc_price` | 64612.01 USD · binance+coinbase+yahoo | 原生现货价格，非 ETF 别名 |
| `btc_etf_flow_verifier` | 独立槽位 · M_USD_proxy · auxiliary_daily | 与价格分离 |
| `treasury_yield` (DGS10) | 4.62 · FRED · age≈53h | 降级观测 |
| `tips_10y_real_yield` | 2.36 · FRED · age≈53h | 降级观测 |
| `dxy_momentum` | 100.82 · Yahoo · fresh | 正常 |
| `nasdaq_breadth` | QQQ/^IXIC proxy · fresh | 风险偏好代理 |

## 路径证据（`macro_path_evidence_latest.json`）

| 字段 | 值 |
|---|---|
| aggregate | support=0, against=3, missing=1, expired=3 |
| top_verdict | 存在反向证据 |
| path_confirmation | insufficient |
| btc_price verifier_key | `btc_price`（非 `btc_etf_flow_verifier`） |

**未输出确认结论** — 符合 C10 硬门禁。

## 桥接上下文（`micro_macro_context.json`）

- `current=true` / `stale=false`（文件新鲜）
- `path_verdict` = 存在反向证据
- `evidence_counts` = 0/3/1/3
- `source_status_summary.path_confirmation` = insufficient
- FRED 影响标注：`last_good_snapshot` → 不得当作实时确认

## 报告回写

- `artifacts/macro_evidence_report_latest.md`
- Obsidian vault：`宏观推导证据闭环报告_最新.md` / `宏观推导证据闭环报告_2026-07-15.md`

## 测试

- `smoke:macro-evidence-loop` 12/12 PASS
- `smoke:micro-macro-context` 8/8 PASS
- `smoke:micro-transmission-engine` 16/16 PASS
- `smoke_macro_evidence_status` PASS

## 未做

- C10-F 页面首屏三分栏（宏观/微观/综合）强化
- C10-G 五情景端到端验收 + FOMC 案例浏览器证据

## 建议下一步

进入 **C10-F**：修复跨层页面表达，禁止「宏观过期 + 微观支持 = confirmed」组合再现。
