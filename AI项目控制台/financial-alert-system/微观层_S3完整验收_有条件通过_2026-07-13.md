---
type: phase-acceptance
project: financial-alert-system
feature: crypto-micro-layer
phase: S3
date: 2026-07-13
status: conditional_pass_RC_blocked
decision: functional_pass_release_gate_failed
tags: [项目, financial-alert-system, 微观层, S3, 验收, 有条件通过, RC阻断]
---

# 微观层 S3：完整验收（有条件通过，2026-07-13）

## 验收结论

> **功能验收通过；研究级 RC 发布门禁未通过。**

微观层专项、原项目全量回归、实时采集和浏览器端到端链路均已通过当前测试；系统成功加载 7 个标的并保持 `advisory-only`，没有产生可执行建议。

但 P2 异常/证据强化尚未正式收口，OKX 实时腿持续失败，Node 采集探针没有加载完整来源可信度模块，校准簿仍依赖 Store 导入，S3 验收脚本与产物也尚未纳入可复现基线。因此：

- **不得标记 S3 COMPLETED；**
- **不得发布研究级 RC；**
- **不得启动 P3 观察期；**
- 下一动作应回到 P2-A、P2-C、P2-E、P2-F 收口阻断项，再重跑 S3。

## 一、验收基线

| 项目 | 当前值 |
|---|---|
| 正式代码 | `F:\financial-alert-system` |
| 分支 | `master` |
| HEAD | `3cd9c14` |
| 产品版本 | `micro-v1.6` |
| Git 状态 | dirty，30项记录 |
| 执行模式 | `advisory-only` |
| 真实下单适配器 | 无（符合边界） |

## 二、微观专项验收

| 套件 | 结果 |
|---|---:|
| `smoke:micro` | 11/11 PASS |
| `smoke:micro-page` | 5/5 PASS |
| `smoke:micro-cal` | 6/6 PASS |
| `smoke:micro-port` | 6/6 PASS |
| `smoke:micro-exec` | 6/6 PASS |
| `smoke:micro-trust` | 6/6 PASS |
| `smoke:micro-contract` | PASS（8类契约、5类夹具） |
| `smoke:micro-s2` | 8/8 PASS |

判断：微观计算、页面结构、校准、组合、执行门禁、来源可信度和统一集成没有代码级阻断。

## 三、原项目全量回归

本轮使用正式套件重新验收，早前 `smoke 12/13` 的 `workbench_p3` 失败已不再出现。

| 门禁 | 最新结果 | 判定 |
|---|---:|---|
| `npm run check` | 6/6 PASS | ✅ |
| `npm run acceptance` | 14/14 PASS | ✅ |
| `npm run smoke` | 13/13 PASS | ✅ |
| 图谱规模 | 128 节点 / 327 边 | ✅ 未破坏 |
| 核心 URL 覆盖 | 116/117，99% | ✅ |

验收产物：`artifacts/last_check.json`、`artifacts/last_acceptance.json`、`artifacts/last_smoke.json`，三者均记录 `pass: true`。

## 四、真实交易所数据

### BTC 多所腿探针

| 来源 | 状态 | funding 周期 | funding | OI 美元名义值 |
|---|---|---:|---:|---:|
| Binance | ✅ | 8h，非假设 | 0.001521% | 约65.92亿美元 |
| Bybit | ✅ | 8h，非假设 | 0.001808% | 约36.74亿美元 |
| OKX | ❌ | — | — | — |
| 聚合 | 2/3健康 | 8h | 0.0016645% | 约102.66亿美元 |

判断：达到默认“至少2家健康来源”的最低条件，但没有达到三所冗余；OKX 仍是明确的可用性缺口。

### 7标的采集

- 采集标的：BTC、ETH、SOL、SUI、DOGE、TAO、ORDI；
- 返回决策：7/7；
- 数据质量：各标的约0.90；
- 结论：7/7 `abstain`，0个 actionable；
- 用时：约27.7秒；
- Node 探针中的 `source_trust` 为 `null`。

`source_trust=null` 的直接原因是 S3 Node 探针只加载 `micro_layer.js`，没有先加载 `micro_source_trust.js`。浏览器运行时按正确顺序加载该模块，因此这是**验收探针覆盖缺口**，但也意味着当前 Node 端到端证据不能证明可信度门禁完整工作。

## 五、浏览器端到端

| 项目 | 结果 |
|---|---|
| 页面加载 | PASS |
| 运行模式 | 服务模式、建议模式、不自动下单 |
| 覆盖标的 | 7 |
| 平均数据质量 | 90% |
| 有效状态节点 | 21 |
| 去重后因子 | 12 |
| 可执行建议 | 0 |
| 主要区域 | 全部存在 |
| 核心脚本 | 全部加载 |
| 页面/控制台错误 | 0 |

浏览器端显示完备度/可信度约 `90% / 100%`，所有标的均正确弃权。此结果证明页面运行链路可用，但与 Node 探针的 `source_trust=null` 存在验收口径差异，必须在P2修正探针后重新确认。

## 六、校准、组合和执行边界

### 校准

| 项目 | 结果 |
|---|---:|
| 校准记录 | 60 |
| `validated` | 6 |
| 主要范围 | SOL/MULTI，48h/72h |
| 24h池化 | 样本外符号翻转，未验证 |

阻断边界：滚动样本外、时间净化、非重叠事件和成本压力尚未完成；页面代码只从 Store 读取 `micro_calibration`，没有自动读取 `artifacts/micro_calibration_book.json`。

### 组合与执行

- 组合专项 6/6 PASS；
- 执行门禁 6/6 PASS；
- 默认 `advisory-only`；
- LLM/force 越权被拒绝；
- 门禁通过后仍不提交订单；
- 当前浏览器 0 个 actionable，符合证据不足时弃权原则。

## 七、Git与发布门禁

### 已满足

- `micro-layer/` 已纳入 Git，共23个已跟踪文件；
- 微观代码本身相对当前 HEAD 没有未提交修改；
- S1基线 `2ba307d` 和状态提交 `3cd9c14` 可回滚。

### 未满足

- 仓库总状态仍有30项记录；
- `project_status.json` 因本次验收重新生成而修改；
- S3浏览器与采集探针仍是未跟踪文件；
- `_s3_*` 验收日志仍散落在仓库根目录；
- P2尚无正式完成记录；
- 当前验收不能由一个干净RC提交完整复现。

## 八、S3门禁矩阵

| S3门禁 | 状态 | 说明 |
|---|---|---|
| 微观专项全部通过 | ✅ | 8套通过 |
| 原项目全量回归 | ✅ | 6/6、14/14、13/13 |
| 真实交易所探针 | 🟡 | 2/3健康；OKX失败 |
| 浏览器实际功能 | ✅ | 7标的、0错误、正确弃权 |
| 校准证据可用 | 🟡 | 6/60；科学性强化未完成 |
| 组合/越权门禁 | ✅ | 专项通过，仍不下单 |
| P2已完成 | ❌ | 尚未形成完成记录 |
| 干净、可复现RC基线 | ❌ | 工作树及S3产物未收口 |

## 九、正式判定

> **S3：CONDITIONAL PASS — FUNCTIONAL PASS / RC BLOCKED**

通过的是：功能、回归、实时页面和安全边界。

未通过的是：P2前置、数据冗余、探针一致性、校准自动加载与科学性、可复现发布基线。

因此本轮只能登记为“有条件通过”，不得登记为“研究级RC已发布”。

## 十、整改顺序

| 顺序 | 归属 | 必须处理 |
|---:|---|---|
| 1 | P2-F | 将S3探针纳入版本控制，验收日志迁入 `artifacts/`，形成可复现命令 |
| 2 | P2-A | 修正Node探针加载顺序，输出真实 `source_trust`；记录OKX降级与连续可用率 |
| 3 | P2-C/E | 自动加载校准簿；补滚动样本外、非重叠和成本压力证据 |
| 4 | P2-F | 隔离无关工作台变更，生成干净RC候选基线 |
| 5 | S3复验 | 重跑专项、全量回归、实时采集、浏览器和Git门禁 |

只有以上完成并复验通过，才能启动 P3 Shadow/Paper 观察期。

## 关联

- [[微观层_并行开发路径与顺序_2026-07-13]]
- [[微观层_S2统一集成_2026-07-13]]
- [[微观层_S1基线与契约冻结_2026-07-13]]
- [[微观层功能实施与验收_2026-07-13]]
- [[数据源可信度与结构夯实_2026-07-13]]
- [[任务进度]]

