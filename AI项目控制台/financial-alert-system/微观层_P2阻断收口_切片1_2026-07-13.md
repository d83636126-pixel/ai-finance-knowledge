---
type: phase-execution
project: financial-alert-system
feature: crypto-micro-layer
phase: P2
date: 2026-07-13
status: in_progress_slice1_done
tags: [项目, financial-alert-system, 微观层, P2, 探针, 验收]
---

# 微观层 P2：阻断项收口 · 切片 1（2026-07-13）

## 本切片范围

按 [[微观层_S3完整验收_有条件通过_2026-07-13]] 整改顺序第 1–2 项：

1. **P2-F**：S3 探针纳入版本控制；验收日志迁入 `artifacts/`；可复现命令
2. **P2-A（探针部分）**：Node 采集探针按浏览器顺序加载 `micro_source_trust.js`，输出真实 `source_trust`；记录 OKX 降级与滚动可用率

未完成本切片：校准自动加载（P2-C/E）、干净 RC 基线隔离无关工作台变更（P2-F 后半）、S3 全量复验。

## 交付物

| 路径 | 作用 |
|---|---|
| `scripts/lib/micro_node_runtime.js` | Node 探针共享引导（内存 Store、模块加载顺序、产物写入） |
| `scripts/probe_micro_p1_legs.js` | 多所腿 + OKX 可用率 + `source_trust` |
| `scripts/probe_micro_collect_s3.js` | 7 标的采集；断言 `source_trust` 模块已加载 |
| `scripts/probe_micro_browser_s3.js` | 浏览器端到端（写 `artifacts/micro_s3_browser.json`） |
| `scripts/run_micro_s3_acceptance.js` | 可复现验收编排 |
| `package.json` | `probe:micro-*` / `accept:micro-s3*` |
| `.gitignore` | `_s3_*.txt`（根目录散落日志不再入库） |

## 可复现命令

```bash
npm run accept:micro-s3:offline   # 8 套微观专项
npm run probe:micro-legs          # 实时三所腿 + OKX 降级记录
npm run probe:micro-collect       # 7 标的 + source_trust
npm run accept:micro-s3           # offline + live（浏览器默认跳过）
MICRO_S3_BROWSER=1 npm run probe:micro-browser   # 需 local_server :8000
```

产物（`artifacts/`，gitignored）：

- `micro_s3_acceptance.json`
- `micro_s3_legs.json`
- `micro_s3_collect.json`
- `micro_exchange_availability.json`
- `micro_s3_browser.json`（可选）

## 实测证据（本机 2026-07-13）

| 检查 | 结果 |
|---|---|
| `accept:micro-s3:offline` | PASS（8/8） |
| `probe:micro-legs` | PASS；Binance/Bybit 健康；OKX degraded；`source_trust` = `source-trust-v1` |
| `probe:micro-collect` | PASS；`n=7`；`source_trust_module_loaded=true`（不再为 null / missing） |

## 缺陷勾销

| ID | 本切片 | 状态 |
|---|---|---|
| S2-D02 | Node 探针输出真实 `source_trust` | ✅ 已修 |
| S2-D01 | OKX 不稳定 + 连续可用率 | 🟡 已记录降级与滚动 `availability_rate`；OKX 仍失败属外部可用性 |
| S2-D09 / D10 | 全量回归与干净基线 | ⏳ 后续切片 |
| S2-D06 | 校准簿自动加载 | ⏳ 下一刀 P2-C/E |

## 下一刀

**P2-C/E**：页面/采集自动加载 `artifacts/micro_calibration_book.json`；补滚动样本外 / 非重叠 / 成本压力证据入口。

## 关联

- [[微观层_并行开发路径与顺序_2026-07-13]]
- [[微观层_S3完整验收_有条件通过_2026-07-13]]
- [[微观层_S2统一集成_2026-07-13]]
