# 微观层重构 C2 执行与验收

- 状态：已实施 / 部分实测（**未宣称「7×24 最小链路成立」**）
- 代码基线：commit `da8fdfb` + dirty；quant-system watcher 已扩 volume/spread
- 修改文件（研究平面 `financial-alert-system`）：
  - `micro-layer/adapters/micro_collect_snapshot_adapter.js`
  - `micro-layer/fixtures/micro_collect_snapshot_fixture.json`
  - `micro-layer/tests/smoke_micro_collect_snapshot.js`
  - `scripts/export_micro_collect_snapshot.js`
  - `scripts/report_micro_collect_continuity.js`
  - `micro-layer/micro_dashboard.js` + `micro-layer/index.html`（「非实时样本」横幅）
  - `package.json`（`smoke:micro-collect-snapshot` / `export:micro-collect-snapshot` / `report:micro-collect-continuity`）
- 修改文件（数据平面 `quant-system`，仓外）：
  - `collectors/crypto_watcher/main.py`（新增 `perp_volume_24h`、`perp_spread_bps`）
- 自动测试：`npm run smoke:micro-collect-snapshot` → 10/10
- 导出：`artifacts/micro_collect_snapshot_latest.json`（live_jsonl；valid≈18 / stale / missing 有明确缺口，不伪造）
- 门禁产物：`artifacts/micro_collect_continuity_gate.json`
  - **30m**：pass（span≈1.3h，last tick 新鲜）
  - **24h**：fail / `c2_declared_24x7: false`
  - **72h**：fail
- 浏览器：fixture 路径显示「非实时样本」已接线；未扩 C3 传导 UI
- 已知问题：
  - 单一 Binance 来源 → `source_trust=provisional`
  - Timescale 仍靠手动 `sync_micro_collect_jsonl.py`（未做自动同步环）
  - 连续 24h 观测尚未完成，**禁止**把「容器 healthy」写成长期稳定性
- 是否满足进入下一阶段：研究读链路与 S2 字段已通，可准备 C3（可用 fixture）；正式状态依赖真实连续数据
- 下一动作：保持 watcher 连续跑满 24h 后再人工签核「7×24 最小链路」；确认后进入 C3 或并行 C4 静态线框

## 清单勾选对照

- [x] S0：可重建 Compose + watcher 运行（本机已健康 >30m；重启后恢复）
- [x] S1 最小：统一 JSONL 契约字段 + 幂等键（既有）；Timescale 迁移脚本保留
- [x] S2 最小字段：价格 / mark / funding / OI / 基差 / **volume** / **spread** / 来源状态
- [x] 只读快照适配器
- [x] 证据字段：`source_timestamp` `ingested_at` `freshness` `source_trust` `missing_reason` `raw_evidence_refs`
- [x] 失效 → stale/missing，不沿用旧值冒充实时
- [x] fixture → 「非实时样本」
- [ ] **24h 连续通过后的正式声明**（未做）
- [ ] **72h 短期稳定**（未做）
