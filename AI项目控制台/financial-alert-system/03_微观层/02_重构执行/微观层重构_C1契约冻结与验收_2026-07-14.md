# 微观层重构 C1 执行与验收

- 状态：已实施 / 已实测
- 代码基线：commit `da8fdfb` + dirty（未动领域算法；仅新增契约层）
- 修改文件：
  - `micro-layer/contracts/micro_transmission_v1.schema.json`
  - `micro-layer/contracts/micro_transmission_contract.js`
  - `micro-layer/contracts/micro_transmission_v1.example.json`
  - `micro-layer/contracts/README.md`
  - `micro-layer/tests/smoke_micro_transmission_contract.js`
  - `package.json`（新增 `smoke:micro-transmission`）
- 数据或契约版本：`micro-transmission-v1`
- 自动测试：`npm run smoke:micro-transmission` 通过（14/14）；夹具五态 + 否定门禁
- 浏览器验证：未做（C1 明确禁止 UI 绑定未冻结字段；确认 `workbench.html` 不存在、dashboard 未读取 v1）
- 人工验证：契约覆盖 scenario / nodes / edges / paths / quality / decision_readiness / outcomes / provenance / evidences
- 已知问题：领域引擎尚未映射现有 `evaluateSnapshot` 输出（属 C3）；连续数据属 C2
- 是否满足进入下一阶段：是（需人工确认进入 C2）
- 下一动作：C2 7×24 最小链路，或按清单并行准备 C4 静态线框（不得与 C2 混提）

## 契约门禁核对

- [x] 节点 ID 唯一
- [x] 边的起止节点均存在
- [x] 核心边声明预期方向、滞后、失效条件
- [x] 判断可追溯到证据 ID
- [x] `synthetic_test_only` 不得进入正式决策准备度
- [x] `executable` 只能为 `false`
- [x] 过期宏观上下文不能标记为当前路径确认
- [x] 示例覆盖确认 / 冲突 / 混合 / 缺失 / 过期
