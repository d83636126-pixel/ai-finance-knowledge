# 微观层重构 C0 现场基线与改动归属

- 状态：已实测 / 条件通过
- 代码基线：`da8fdfb`（`fix: close research workbench P1 P2 gates`）+ dirty 工作区
- 分支：`master`（跟踪 `origin/master`）
- 环境：Node `v25.2.1`，npm `11.6.2`，页面端口 `8000`（`local_server.js`）
- 数据或契约版本：尚未冻结 `micro-transmission-v1`（属 C1）
- C0 约束：不修改业务逻辑；不 reset/checkout/clean；不提交/push；不进入 C1

## 结论

当前 dirty 工作区中的宏微观绑定、事件账本、严格门禁、abstain 解释与离线验收相关模块，**已通过 C0 规定的专项测试与语法检查**。这些能力应视为“代码存在且本地实测通过、尚未形成干净提交/研究级 RC”，后续重构必须绕行保护，不得清理覆盖。

正式校准簿：`artifacts/micro_calibration_book.json` 存在；P3 状态字段 `formal_book_unchanged=true`；C0 未发现意外改写迹象。

## 修改文件（本切片实际写入）

仅新增基线证据，未改领域代码：

- `artifacts/micro_redesign_baseline.json`
- `artifacts/_c0_smoke_raw.json`
- 本文件：`微观层重构_C0现场基线与改动归属_2026-07-14.md`

## Dirty 改动归属

### 宏微观绑定

- `micro-layer/micro_macro_bind.js`（未跟踪）
- `micro-layer/tests/smoke_micro_macro_bind.js`（未跟踪）
- `scripts/bridge_macro_to_micro_context.js`
- `scripts/export_micro_macro_context.js`
- `scripts/build_macro_path_evidence.js`
- `scripts/lib/macro_evidence_loop.js`
- `scripts/run_micro_p3_observation.js`（写入 `macro_bind` / evidence jsonl）
- `micro-layer/micro_layer.js` / `micro_dashboard.js`（绑定与过期门禁）

### 事件账本

- `micro-layer/micro_event_ledger.js`
- `micro-layer/tests/smoke_micro_event_ledger.js`
- `scripts/run_micro_event_ledger.js`
- `scripts/resolve_micro_event_ledger.js`

### 严格校准门禁

- `micro-layer/micro_calibration_gates.js`
- `scripts/preview_micro_calibration_gates.js`
- `scripts/solidify_micro_calibration_gate_status.js`

### 连续采集（研究面）

- `scripts/run_micro_continuous_collect.js`
- `scripts/run_micro_continuous_collect_cmd.cmd`
- `scripts/install_micro_continuous_schedule.ps1`
- `scripts/lib/micro_node_runtime.js`

### 页面 / 运行时

- `micro-layer/index.html`、`index.html`
- `micro-layer/micro_dashboard.js`、`micro_layer.js`、`micro_source_trust.js`、`micro_execution_gate.js`
- `package.json` 与对应 smoke 测试更新
- `static/data.js`、`static/ingest/verifier_data_fetchers.js`（OKX/ETF）

### 保留但非微观重构核心（勿误删）

- `static/propagation_*.js`、`static/scenario_reasoner.js`、`static/ingest/inbox_store.js`
- `scripts/perf_research_workbench.js`、`scripts/smoke_research_workbench_p4.js`、`project_status.json`
- `_workbench_check_logs/`、`workbench_full_check.json`（不建议随微观切片提交）

## 基线矩阵

| 能力 | 判定 | 说明 |
|---|---|---|
| 宏微观绑定 | 已实测 | bind/context smoke 通过 |
| 事件账本 | 已实测 | ledger smoke 通过 |
| 严格校准门禁 | 已实测 | micro smoke 含降级断言 |
| abstain 三态 / 页面接线 | 已实测 | micro/page smoke 通过；无人工视觉签核 |
| S3 离线验收 | 已实测 | `accept:micro-s3:offline` 通过 |
| P3 只观察 | 条件通过 | smoke 通过；28 天与连续 3 次自动成功未完成 |
| 研究面连续采集 | 条件通过 | 脚本+调度存在；24h 门禁未宣称 |
| OKX/ETF 数据质量 | 条件通过 | 代码实测进 offline；外网源仍常降级 |
| 路径中心传导工作台 | 未实测 | C1+ 才开始 |
| 仓外 watcher/Timescale | 条件通过 | 不计入本仓库 Git 发布基线 |

## 自动测试

全部退出码 0：

```text
npm run smoke:micro
npm run smoke:micro-page
npm run smoke:micro-contract
npm run smoke:micro-ledger
npm run smoke:micro-macro-bind
npm run smoke:micro-macro-context
npm run smoke:micro-p3
npm run accept:micro-s3:offline
```

语法检查（`node --check`）通过：gates / ledger / macro_bind / micro_layer / dashboard / continuous collect / event ledger scripts / bridge。

## 浏览器验证

- C0 未要求浏览器探针；未做 UI 视觉签核。
- 启动入口：`http://127.0.0.1:8000/micro-layer/index.html`

## 人工验证

- 已确认：dirty 归属可分类；正式簿未见意外改写。
- 未做：10/20/30 秒路径工作台体验（功能尚未切换）。

## 已知问题

- 工作区仍 dirty；HEAD `da8fdfb` 不含当前能力全集。
- OKX 外网经常 `network_failure`；ETF Farside 不稳定——属于来源现实，不是 C0 失败。
- 24h / 72h / 28 天真实运行门禁均未通过（也未宣称）。
- `_workbench_check_logs/` 等临时产物需与微观切片隔离。

## 是否满足进入下一阶段

是（建议进入 **C1**，需人工确认）。

C0 目标是保护与识别，不是继续写功能；未开始 C1 契约。

## 下一动作

人工确认后，再执行 C1：冻结 `micro-transmission-v1` 契约（仍不得 reset/clean 当前 dirty 成果）。
